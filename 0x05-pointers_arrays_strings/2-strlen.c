#include "main.h"

/**
 * _strlen - length of a string
 * @s: - input the string to measure the length
 * @length: - variable to output the length
 */

int _strlen(char *s)
{
	char *s;
    int length;

    length = _strlen(*s);

    return (length);
}
