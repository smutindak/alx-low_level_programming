#include "main.h"
#include <stdio.h>
/**
 * _strlen - length of a string
 * @s: - input the string to measure the length
 * Return: Always 0.
 */

int _strlen(char *s)
{

	int l;

	l = strlen(*s);
	return (l);
}
