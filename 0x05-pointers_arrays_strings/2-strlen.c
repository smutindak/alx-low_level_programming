#include "main.h"
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

/**
 * _strlen - length of a string
 * @s: - input the string to measure the length
 * Return: Always 0.
 */

int _strlen(char *s)
{

	int lenth0 = (int)s;
	int length1;

	length = strlen(length0);
	return (length);
}
