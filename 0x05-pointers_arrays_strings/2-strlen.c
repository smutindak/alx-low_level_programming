#include "main.h"

/**
 * _strlen - length of a string
 * @s: - input the string to measure the length
 * Return: Always 0.
 */

int _strlen(char *s)
{
	char *s;

	int l;

	l = _strlen(*s);
	return (l);
}
